import styled from 'styled-components';

/** The Container component is used to wrap the content inside all sections to unify content width across the entire document */
const Container = styled.div`
  width: 100%;
  max-width: 960px;
  margin: 0 auto;
`;

export default Container;